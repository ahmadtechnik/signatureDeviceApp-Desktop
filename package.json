{
  "name": "electron-quick-start",
  "version": "1.0.0",
  "description": "A minimal Electron application",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "packTo64bit": "electron-packager . Signature --overwrite --asar=true --platform=win32 --arch=x64 --icon='./assets/mainIco.ico' --prune=true --out=release-builds --version-string.CompanyName=CE --version-string.FileDescription=CE --version-string.ProductName='SignatureDeviceClient'"
  },
  "author": "Ahmad Koudier",
  "devDependencies": {
    "electron": "^5.0.2",
    "electron-packager": "^13.1.1"
  },
  "dependencies": {
    "jquery": "^3.4.1"
  }
}
